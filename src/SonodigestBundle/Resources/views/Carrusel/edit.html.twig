{% extends 'SonodigestBundle:Layout:layoutpanel.html.twig' %}

{% block bod -%}
    <h1>Carrusel edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_carrusel') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
