{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Carrusel list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>              
                <th>Carrusel</th>
                <th>Imagen</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_carrusel_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                 <td>{{ entity.tipoCarrusel }}</td>
                <td><img class="embed-responsive-item" itemprop="thumbnail" src="{{ asset('Photos/carrusel/')}}{{entity.imagen}}" alt="dummy image"> </td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_carrusel_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_carrusel_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_carrusel_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
