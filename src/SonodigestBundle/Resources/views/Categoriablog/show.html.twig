{% extends 'SonodigestBundle:Layout:layoutpanel.html.twig' %}
{% block titulo %}Crear Categoria Blog{% endblock %}
{% block bod -%}

    <div class="container">
        <div class="row">
            {#<div class="col-md-3"></div>#}
            <div class="col-md-12">
                <table class="table-bordered" style="margin: 0 auto; width: 60%;" >
                    <tbody>
                        <tr>
                            <th class="text-center">Id</th>
                            <td class="text-center">{{ entity.id }}</td>
                        </tr>
                        <tr>
                            <th class="text-center">Nombre</th>
                            <td class="text-center">{{ entity.nombre }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            {#<div class="col-md-4"></div>         #}
            <div class="clearfix"></div>

            <div class="col-md-3"></div>
            <div class="col-md-3">
                <a href="{{ path('admin_categoriablog') }}"><div class="botonpanel">Regresar a la lista</div></a>
            </div>
            <div class="col-md-3">
                <a href="{{ path('admin_categoriablog_edit', { 'id': entity.id }) }}"><div class="botonpanel">Editar categoria</div></a>
            </div>
            <div class="col-md-3"></div>

            <!--Area de botonces-->
            
        </div>
    </div>
{% endblock %}

{#{% extends '::base.html.twig' %}

{% block body -%}
    <h1>categoriablog</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_categoriablog') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_categoriablog_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
#}