<!-- LADO IZQUIERDO -->
				<div class="col-md-4">
					<!-- IGNORAR SOLO DE BACKUP ESTILO DE BOTON PRINCIPAL DE SERVICIO
					<div style="width: 100%; max-width: 100%; height: 50px; background-color: #76B9C4;"> 
						<a class="pull-left text-uppercase" style="color: #FFFFFF; line-height: 50px; margin-left: 10px;font-family: 'Montserrat', sans-serif; font-weight: 400;" >Cirujias Laser</a> 
						<a class="pull-right"><img src="recursos/img/arrow-down.png" style="width: 16px; margin-top: 19px; margin-right: 10px;"></a> 
					</div> -->
                                    
                                        <div style="height: 17px;"><!-- SPACER --></div>
					<div class="panel-group" id="accordion" role="tablist" aria-multiselectable="true">
                                            {% for key, categoria in categorias %}
                                            <div class="panel panel-default">
                                            
                                                <div class="panel-heading" role="tab" id="heading{{categoria.id}}" style="width: 100%; max-width: 100%; height: 50px; background-color: #76B9C4; line-height: 30px; font-family: 'Montserrat', sans-serif; font-weight: 400;">
                                                    <a style="color: #FFF;" charset="pull-left" role="button" data-toggle="collapse" data-parent="#accordion" href="#collapse{{categoria.id}}" aria-expanded="true" aria-controls="collapse{{categoria.id}}">
                                                      {{categoria.nombre}}
                                                    </a>
                                                    <a class="pull-right"><img src="{{ asset('Photos/ciruyproc/arrow-down.png')}}" style="width: 16px; margin-right: 10px;"></a> 
                                                </div>
                                                <div id="collapse{{categoria.id}}" class="panel-collapse collapse {% if key==0%}in {% endif %}" role="tabpanel" aria-labelledby="heading{{categoria.id}}">
                                                    <ul class="list-group">
                                                        {% for subcategoria in categoria.subcategoria %}
                                                            <a id="{{subcategoria.id}}" class="subcategoriaLink" href="{{path('get_subcategoria',{id:subcategoria.id})}}" aria-expanded="true" aria-controls="collapse{{categoria.id}}">    
                                                                <li class="list-group-item">{{subcategoria.titulo}}</li>
                                                            </a>
                                                        {% endfor %}
                                                    </ul>
                                                </div>
                                            
                                            </div>
                                            {% endfor %}
					</div>
                                    
				</div>