{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Imagenblog list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Imagen1</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_imagenblog_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.imagen1 }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_imagenblog_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_imagenblog_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_imagenblog_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
