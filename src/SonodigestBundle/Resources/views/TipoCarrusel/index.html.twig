{% extends 'SonodigestBundle:Layout:layoutpanel.html.twig' %}

{% block bod -%}
    <h1>Carrusel Principal</h1>
 
    <table class="records_list">
    {#    <thead>
            <tr>
                <th>Id</th>
                <th>Detalle</th>
                <th>Estado</th> 
                <th>Actions</th>
            </tr>
        </thead>  #}
        <tbody>
        {% for entity in entities %}
            <tr>
            {#    <td><a href="{{ path('admin_tipocarrusel_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.detalle }}</td>
               <td>{{ entity.estado }}</td> 
                <td>
                <ul>
                  <li>
                        <a href="{{ path('admin_tipocarrusel_show', { 'id': entity.id }) }}">show</a>
                    </li> #}
                    <li> 
                        <a href="{{ path('admin_tipocarrusel_edit', { 'id': entity.id }) }}">Modificar Imagenes</a>
                    </li>
                </ul>  
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table> 

  {#      <ul>
        <li>
            <a href="{{ path('admin_tipocarrusel_new') }}">
                Create a new entry
            </a>
        </li>
    </ul> #}
    {% endblock %}
