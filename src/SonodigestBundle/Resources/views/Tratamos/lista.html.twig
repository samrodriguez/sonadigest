

<div class="container" style="margin-top: 30px;">
                <div class="row">
                
                {% for key, problema in problemas %}
                    {% if loop.last %}
                        <div class="col-md-3"></div>
                        <a href="{{path('admin_problemadetalle',{id:problema.id})}}"> 
                            <div class="col-md-6" style="max-width: 100%;margin-top: 20px;">
                                <img style="width: 100%;" src="{{ asset('Photos/problema/')}}{{problema.foto}}">
                            </div>
                        </a>
                        <div class="col-md-3"></div>
                    {% else %}
                        <a href="{{path('admin_problemadetalle',{id:problema.id})}}"> 
                            <div class="col-md-6" style="max-width: 100%; margin-top: 20px;">
                                <img style="width: 100%;" src="{{ asset('Photos/problema/')}}{{problema.foto}}">
                            </div>
                        </a>
                    {% endif %}
                {% endfor %}
            
                </div>
            </div>
                
                